//Write a js function to capitalize a first letter

// const capital=(string)=>{
// return string[0].toUpperCase()+string.slice(1);
// };
// console.log(capital("Js is a best"));

const capital=(string)=>{
    return `${string[0].toUpperCase()}${string.slice(1)}`;
    };
    console.log(capital("Js is a best"));