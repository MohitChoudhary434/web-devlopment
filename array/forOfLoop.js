//iterate an array by using for Of loop also in this loop we will used a break keyword.

const arr=[1,2,3];
for(let  num of arr)
{
    console.log(num);
}

//iterate an array by using a normal for loop
const arr1=[1,2,3,4];
for(let  num=0;num<arr1.length;num++)
{
    console.log(arr1[num]);
}